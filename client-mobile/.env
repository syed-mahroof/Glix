# API URL is now auto-detected based on platform (web=localhost, mobile=Metro bundler IP).
# Uncomment and set this ONLY if you need to force a specific URL:
# EXPO_PUBLIC_API_URL=http://192.168.1.2:8000/api/v1

# Sign in with Google (lib/socialAuth.ts) — the WEB-type OAuth Client ID
# from Google Cloud Console (not the iOS or Android one). Required even
# for the native mobile flow — see the setup guide in PROJECT_STATUS.md
# Phase 27 / AUDIT.md for where to get this.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=374430247793-c414mmpilsl3u8l4ae2fg2fss244v3lj.apps.googleusercontent.com
